/**
 * Work-context capture — one shared implementation in @ayo-dev/core/node
 * (also used by the MCP server), re-exported for the CLI's callers.
 */

export { captureContext } from "@ayo-dev/core/node";
